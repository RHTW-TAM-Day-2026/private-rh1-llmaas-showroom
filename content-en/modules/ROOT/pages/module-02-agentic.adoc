:imagesdir: ../assets/images
:page-aliases: main@modules:ROOT:module-02-agentic.adoc

[#agentic-ai]
= System Administration with Agentic AI  

Next, you’ll step into the role of a **Site Reliability Engineer (SRE) or DevOps practitioner**, focusing on observability, diagnostics, and operational scale.

You won't be tackling this alone—you’ll collaborate with agentic AI tools to navigate your OpenShift environment in real time, mirroring how modern SRE teams build insight-driven automation.


== Getting Familiar with Your Environment

To kick things off, let's get acquainted with our environment and its key components.

Your central hub will be the OpenShift cluster web console:

// NOTE: If you are accessing these instructions through our Red Hat Demo Platform workshop, the information below will render properly with unique values. If you are accessing the instructions separately for your own cluster, from the GitHub repository, you will instead see placeholder values.

**Console URL**: https://console-openshift-console.{openshift_cluster_ingress_domain}/[https://console-openshift-console.{openshift_cluster_ingress_domain}/,window=_blank].

**Login Credentials**:

. **User**
+
[source,console,role=execute,subs=attributes+]
----
{user}
----
. **Password**
+
[source,console,role=execute,subs=attributes+]
----
{password}
----

Here are the key environments you will interact with and navigate across during this workshop:

* **Cluster Argo CD**
** You may use this app to view our GitOps application deployments. This may be useful if you need to check the health of any of our apps.
* **Grafana**
** A dashboard interface that will show your model usage metrics.
* **Red Hat OpenShift AI**
** Where you can view and manage your model deployments and other MLOps/LLMOps tasks and agentic resources.
* **Red Hat OpenShift Dev Spaces**
** Where you can go into your developer persona and leverage your model as a service setup for coding tasks.

You may find and go to these applications via our navigation button in the web console:

image::apps-menu.png[]

Now that you know your way around, let's dive in!

== Why Agentic AI for Admins?

Think of agentic AI as your copilot for cluster operations - ready to answer complex infrastructure questions using reasoning and real-time data.

Instead of navigating complex dashboards, YAML, and shell scripts, you'll try something new:

* “What’s consuming the most memory in our dev cluster right now?”

* “Were there any pod crashes in the last 30 minutes?”

* “Why did that Job fail this morning?”

These natural-language prompts get translated into real queries and results. The outcome? Faster issue resolution, increased system transparency, and lower barrier to operational insights for both technical and non-technical users.

That's an example of the power of agentic AI.

== Meet Your New Toolkit

In this module, we'll use a preconfigured stack that includes:

* **https://github.com/meta-llama/llama-stack[OGX (Llama Stack)]** - a flexible open-source framework developed by Meta to simplify the creation and deployment of advanced AI applications.
** OGX(Llama Stack) provides a unified API layer for inference, RAG, agents, tools, safety, evals and telemetry.
** **Why It Matters for Enterprises**:
*** Codifies best practices across Gen AI tools
*** Enables reproducible, explainable, and extensible AI workflows
*** In short, it lets you focus on value creation, not toolchain assembly.

IMPORTANT: You may use frameworks like LangChain or CrewAI in conjunction with OGX  in OpenShift AI. These tools, together, help you build agentic AI workflows with reasoning, tool use, and orchestration. OGX  is Red Hat's recommended, and supported, framework for standardizing and interweaving disparate pieces of an agentic system into one observable, secure stack.

* **Kubernetes MCP Server**  - allows LLM agent to interact with your live OpenShift cluster and deployed resources.

* **Slack MCP Server** - allows LLM agent to interact with a Slack workspace. It will be used to interact with a specific Slack workspace for which authorization had been configured.

== Tying it Back to our MaaS Endpoint

You can connect a OGX (Llama Stack) server deployment to any model endpoint(s) you desire. For the workshop, OGX server instance is configured with the Qwen model and two MCP servers. 

== Test model + MCP server functionality in the playground

* To access the Red Hat OpenShift AI dashboard, return to the main OpenShift web console and open the dashboard from there, as you cannot navigate to it directly from Dev Spaces:

image::nav-rhoai-2.png[]

* Select `AI asset endpoints` from the navigation menu.

image::nav-aiassets.png[]

* Change project back to `Workspace userx` accordingly.

image::aiassets-changeproject.png[]

* View MCP servers

image::genaistudio-mcpservers.png[]

* See what is available to you. Select both MCP servers and then click `Try in Playground`.

image::mcp-try-playground.png[]

=== Using the playground

Welcome to the playground, where you can test your models, assets, and other connections in a user-friendly dashboard interface before integrating into your applications directly. 

As a developer, you likely aren't using the playground as your primary way of connecting to your resources. However, it's a great way to test and validate that the model(s) you selected, the MCP servers you configured, and anything else you are integrating work together properly and as expected before you invest in further development.

In the playground, you will see a chat interface. One model is preselected along with a default system prompt. If you would like to change or adjust the system prompt or temperature, feel free to do so.

NOTE: The temperature setting correlates to how creative the model is permitted to be. It is recommended to keep this setting low to ensure the model is as accurate when answering you as possible, particularly when using in conjunction with MCP servers.

image::playground.png[]

Before you begin chatting with the model and MCP servers, authorize the MCP servers in the playground by clicking the lock buttons:

image::authorize-mcp.png[]

You will see the following pop-up each time you click the lock button. Click `Close`:

image::mcp-auth-close.png[]

You are now setup to use the chat interface to test the model and its functionality with our MCP servers.

NOTE: If you refresh the page or have to access the playground again, you will need to reset your settings as they will have been set back to the defaults.

.View Tools

You may view the MCP servers' available tools by clicking on the small tool icons next to the server names:

image::view-mcp-tools.png[]

== Investigate our OpenShift Resources

The tool information you just viewed will give you guidance into how to interact with the model in chat to activate the tool calls correctly. You will need to utilize the right keywords so that you're engaging with the appropriate tools.

In the Playground chat, enter:

[source,console,role=execute,subs=attributes+]
----
List all pods in the wksp-{user} namespace.
----

Response output will vary slightly, but you should see it correctly list out two active pods. One for the Llama Stack playground and the other for the Dev Spaces workspace.

Let's try something else and post a message containing your name to the Slack workspace: 

[source,console,role=execute,subs=attributes+]
----
Get logs for the lsd-genai-playground pod in the wksp-{user} namespace.
----

Feel free to experiment further with the tools available.

=== Post a Message to the Slack Workspace

With the Slack MCP Server connected to Llama Stack, you can extend your agentic AI experience beyond Kubernetes and into team collaboration tools (among many other possibilities!).

This MCP server bridges your AI agent with a Slack workspace to fetch approved data.

**Why this matters:**

* SREs and DevOps teams often work across multiple collaboration channels.

* By giving your AI visibility into Slack, you can use natural language to check team communication spaces without switching tools.

* This is a safe, read-only example — no messages are read or posted in this activity.

In the LlamaStack Playground chat interface, type:

[source,console,role=execute,subs=attributes+]
----
List all Slack channels in our Slack workspace.
----

Now, let's post a message to the Slack workspace:

[source,console,role=execute,subs=attributes+]
----
Post a message to the #rh-tw-tam-day-2026 channel: "Hi from <insert name>".
----

==== Send logs to Slack

Now, let’s explore a practical, real-world use case. In production, you would typically rely on robust automation to send logs or other cluster information from OpenShift to Slack rather than using a chat interface like this. However, this exercise demonstrates how agentic AI can help streamline everyday operational tasks.


Let's send the logs for the lsd-genai-playground pod to the #rh-tw-tam-day-2026 channel.

[source,console,role=execute,subs=attributes+]
----
Post the logs for the lsd-genai-playground pod in the wksp-{user} namespace to the #rh-tw-tam-day-2026 channel in Slack.
----

== Summary: What You Did

In this module, you:

* Integrated your own LLM with a tool-using agent.
* Explored OpenShift resources with natural language
* Interacted with a Slack workspace using natural language
* Added AI guardrails with input/output shields.

You just used AI to reduce operational complexity and speed up workflows!
